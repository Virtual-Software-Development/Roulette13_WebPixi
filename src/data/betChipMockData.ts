// Denominaciones de ficha -- no existe configuración de mesa real todavía (sin endpoint de
// límites/fichas en el backend, ver investigación). Reemplazar por la config real (ej.
// GET /api/tableConfig) cuando exista, sin tocar ChipSelector.tsx.
export const CHIP_DENOMINATIONS = [1, 5, 25, 100, 500]
